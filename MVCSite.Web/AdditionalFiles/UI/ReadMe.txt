Most JS/CSS/font files are under "assets" folder under root

Our front end uses LESS framework, so we need to complie changed less files by following the Compliation instruction.

--------Angular 2-------
custom typescript files are under foler "tsScripts"
After compiling typescripts into javascripts, files will be moved into foler "libs". 
Confirguation is specified by file "systemjs.config.js" under the root
